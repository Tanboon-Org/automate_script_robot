# Robot Framework automation suite — WNW funeral-wreath e-commerce (POM)
# Converted from the Playwright suite in ../automation-tester-with-ai
# ทุกตัว pin เวอร์ชันตายตัวเพื่อให้ CI reproducible — อัปเกรดแบบตั้งใจเท่านั้น
robotframework==7.1.1
robotframework-seleniumlibrary==6.6.1
# YAML variable files (resources/variables/*.yaml)
robotframework-pythonlibcore==4.6.0
PyYAML==6.0.3
# Selenium driver management (Selenium Manager ships with selenium>=4.6 — no manual chromedriver)
selenium==4.45.0
# HTTP client — ใช้โดย libraries/api_library.py (tests/api) และ scripts/link_check.py
requests==2.34.2
# Gemini AI analysis ของผลรัน Robot (scripts/analyze_results.py)
google-genai==2.10.0
